export function mosaicNumImagesSelector(state) {
    return state.mosaic.numImages;
}

export function mosaicTilingImageScaleSelector(state) {
    return state.mosaic.tilingImageScale;
}

export function mosaicOutputImageScaleSelector(state) {
    return state.mosaic.outputImageScale;
}